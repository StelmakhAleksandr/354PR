#pragma once

#include <iostream>
#include <vector>

class SortHelper {
public:
    SortHelper() = default;
    // generate
    // display
    // track time
    void display(const std::vector<int>& arr);

private:
};
